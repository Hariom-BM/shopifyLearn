    {% case media.media_type %}
      {% when 'image' %}
{% for image in product.images %}
          <div class="swiper-slide">
          <img class="lazyload"
                 loading="lazy"
                 src="{{ 'loading-image.png' | asset_url }}"
                 data-src="{{ image | img_url: '750x999', crop: 'center' }}" />
            <noscript>
              <img src="{{ image | img_url: '750x999', crop: 'center' }}" />
            </noscript>
          </div>
        {% endfor %}
      {% when 'external_video' %}
 <div class="swiper-slide">
        <div class="product-single__media" style="padding-top: {{ 1 | divided_by: media.aspect_ratio | times: 100}}%;" data-media-id="{{ media.id }}">
          {{ media | external_video_tag }}
        </div>
</div>
      {% when 'video' %}
    <div class="swiper-slide">
        <div class="product-single__media" data-media-id="{{ media.id }}">  
          
          {{ media | video_tag: controls: 'true' }}
          
        </div>
      </div>
      {% when 'model' %}
       <div class="swiper-slide">
        <div class="product-single__media" style="padding-top: 100%" data-media-id="{{ media.id }}">
          {{ media | model_viewer_tag }}
        </div>
      </div>
      {% else %}
          <div class="swiper-slide">
        <div class="product-single__media" style="padding-top: 100%;" data-media-id="{{ media.id }}">
          {{ media | media_tag }}
        </div>
         </div>
    {% endcase %}